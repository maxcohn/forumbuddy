{{define "header"}}
<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="/static/style.css" />


  <!--<link rel="stylesheet" href="/static/bulma.min.css" />-->

	<title>change me</title>
</head>
<body>
	<header>
		ForumBuddy | {{if .IsLoggedIn}}<a href="/user/{{.CurrentUser.Username}}">{{.CurrentUser.Username}}</a>{{else}}<a href="/login">Login</a>{{end}}
	</header>

	<div class="content">
{{end}}